
 QuestionMaker.
 ==============
bitmap.h -     bitmap validation and manipulation,file handling functions.
resource.h -   contains the ID's to the resources.
validate.h -   contains the functions that are needed to validate the forum.
bitmap.cpp -   Implementation of the bitmap.h
main.cpp   -   The C++ source file that contains the WinMain and the main Window
               procedure,and the child windows and application specific datastructures.


Other Dependencies - depends on the question.h/test.h helders from the main TestEngine
project.

Dependencies
 Standarded template library.
 Win32 API , windows.h gdi32.h

